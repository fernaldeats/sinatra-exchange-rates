<h1>Currency pairs</h1>
<ul>
  <% @currency_keys.each do |a_currency| %>
    <li>
      <a href="/<%= a_currency %>">
       <%= "Convert 1 #{a_currency} to..." %>
      </a>
    </li>
  <% end %>
</ul>
